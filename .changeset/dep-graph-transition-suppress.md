---
'@backstage/core-components': patch
---

Avoided jarring layout animation in `DependencyGraph` when input parameters change after the initial render, such as when toggling options in full-screen mode.
